% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_leaky_onward.R
\name{estimate_leaky_onward}
\alias{estimate_leaky_onward}
\title{Estimate the leaky onward burden of OBV-averted infections (post-hoc)}
\usage{
estimate_leaky_onward(
  prevented_completed,
  params,
  residual_transmissibility,
  n_replicates = 1L,
  max_tree_size = 1e+05,
  seed = NULL,
  return_trees = FALSE
)
}
\arguments{
\item{prevented_completed}{The \code{prevented_completed} data frame returned by
\code{\link{branching_process_main}()} (the averted index infections, each
already carrying its counterfactual natural history). \code{NULL} or a 0-row
frame is allowed and yields an all-zero result.}

\item{params}{Named list of the transmission / natural-history / OBV parameters
the run used. The convenient source is \code{out$sim_info$params} from the same
\code{\link{branching_process_main}()} call; it can also be hand-built with the
same names as that function's arguments.}

\item{residual_transmissibility}{Single numeric in \code{[0, 1]}. Fraction of
normal transmission an engaged (leaky) individual still produces.}

\item{n_replicates}{Positive integer. Number of independent forward-simulation
replicates; the onward burden is stochastic, so the result reports its
distribution rather than a single value. Defaults to 1.}

\item{max_tree_size}{Positive integer. Per-replicate cap on the total number of
simulated infections (seeds + descendants); expansion stops if it is reached and
a warning is issued (so a supercritical \code{residual_transmissibility} near 1
cannot run away silently). Defaults to 1e5.}

\item{seed}{Optional integer. RNG seed for reproducibility of the replicates.}

\item{return_trees}{Logical. If TRUE, the full simulated infection frame for each
replicate is returned in \code{$trees} (seeds first, \code{engaged} column
flagging leaky cases). Defaults to FALSE.}
}
\value{
A named list:
  \describe{
    \item{\code{residual_transmissibility}, \code{n_replicates}}{Echoed inputs.}
    \item{\code{n_seeds}}{Number of averted index infections replayed (rows of
      \code{prevented_completed}).}
    \item{\code{seed_deaths}}{Fixed number of would-be deaths among those index
      infections (equals \code{prevented_deaths}); added back on every replicate.}
    \item{\code{per_replicate}}{Data frame, one row per replicate, with
      \code{onward_infections} / \code{onward_deaths} (descendants only),
      \code{added_infections} / \code{added_deaths} (index cases + descendants),
      and \code{capped} (did the replicate hit \code{max_tree_size}).}
    \item{\code{summary}}{Means and 2.5/50/97.5\% quantiles of the four count
      columns across replicates.}
    \item{\code{trees}}{Present only when \code{return_trees = TRUE}: a list of
      the per-replicate infection frames.}
  }
}
\description{
A post-hoc sensitivity estimator for the transmission impact of obeldesivir
(OBV) PEP under a \emph{leaky} (reduced-infectiousness) reinterpretation of its
efficacy, addressing the concern that treating OBV as fully sterilizing is
optimistic. It runs \strong{outside} \code{\link{branching_process_main}()},
consuming that run's \code{prevented_completed} frame and parameter bundle, and
never perturbs the original trajectory or its RNG stream.
}
\details{
\strong{What it does.} The infections OBV removed in the main run
(\code{prevented_completed}) are treated as the first cohort of \emph{engaged}
cases: they \emph{did} become infected, but transmit at only
\code{residual_transmissibility} (r) of the normal rate. Each such case is
forward-simulated through the same offspring (community / hospital / funeral) and
\code{\link{complete_offspring_info}()} machinery as the main model. Descendants
are ordinary infections and transmit fully -- but any descendant on whom the OBV
gate \emph{engages} (the same coverage / adherence / efficacy draws) becomes
leaky in turn, transmitting at r, recursively at every generation. Because fiber
currently feeds no susceptible / HCW depletion back into transmission, each
averted infection's subtree is an independent branching process, so this post-hoc
replay is an unbiased estimate of the onward burden.

\strong{Interpreting r.} r = 0 reproduces the main model's transmission
behaviour (engaged individuals transmit nothing) yet, unlike the sterilizing
model, still counts them as cases that can die -- so at r = 0 the added
infections equal the number of averted index infections and the added deaths
equal \code{prevented_deaths}, with no onward chains. r = 1 means OBV has no
transmission effect, approaching the no-OBV world. Sweeping r brackets the
reviewer's "partial protection / reduced infectiousness" concern.

\strong{Accounting (decision: index cases are added back).} The averted index
infections are counted as realised leaky cases and their (already-resolved,
fixed) deaths are added back, alongside their stochastic onward chains. r scales
an engaged individual's transmission on \emph{all} routes -- community, hospital
and funeral -- since it represents that individual's overall residual
infectiousness.
}
