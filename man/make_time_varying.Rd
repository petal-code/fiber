% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_time_varying.R
\name{make_time_varying}
\alias{make_time_varying}
\title{Create a Time-Varying Parameter Function}
\usage{
make_time_varying(times, values, method = "linear")
}
\arguments{
\item{times}{Numeric vector of time points (non-decreasing, length >= 2).}

\item{values}{Numeric vector of corresponding values (same length as \code{times}).}

\item{method}{Character string: \code{"linear"} (default) for piecewise-linear
interpolation, or \code{"constant"} for a left-continuous step function
(each value holds until the next breakpoint).}
}
\value{
A function \code{f(t)} that accepts one or more numeric times and returns the
interpolated or stepped value(s). Values are clamped to \code{values[1]} for
\code{t < min(times)} and \code{values[length(values)]} for \code{t > max(times)}.
The returned function has class \code{c("time_varying_fn", "function")}.
}
\description{
Constructs a function of time from a set of breakpoints, using either
piecewise-linear interpolation or a step (constant) function.
This is a convenience wrapper around \code{\link[stats:approxfun]{stats::approxfun()}} designed for
use with time-varying simulation parameters such as
\code{prob_hospitalised_genPop} and \code{hospitalisation_delay_factor}.
}
\examples{
# Piecewise-linear: probability rises from 0.05 to 0.5 over 60 days
prob_hosp <- make_time_varying(
  times  = c(0, 14, 30, 60),
  values = c(0.05, 0.1, 0.3, 0.5)
)
prob_hosp(20)
prob_hosp(c(10, 20, 40))

# Step function: constant between changepoints
prob_hosp_step <- make_time_varying(
  times  = c(0, 14, 30, 60),
  values = c(0.05, 0.1, 0.3, 0.5),
  method = "constant"
)
prob_hosp_step(20)

}
